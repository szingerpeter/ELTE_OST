## Streaming clustering pipeline

### TODO before running:
- Build the docker image inside the flink2.12 directory:
```docker build ./flink2.12 -t flink:1.12```

## Investigate:
Investigate the job via localhost:8086